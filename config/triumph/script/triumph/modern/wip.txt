setIcon(<buildcraftlib:guide_note>)
setTitle("Coming Soon...")
setDescription("The Modern Era isn't quite ready yet, but stay tuned for future updates!")

setParent("triumph:modern/root")

setShowToast(false)
setAnnounceToChat(false)

addCriteria("main", "minecraft:impossible")