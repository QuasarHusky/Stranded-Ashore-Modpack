setIcon(<buildcraftlib:guide_note>)
setTitle("Coming Soon...")
setDescription("The Interstellar Age isn't quite ready yet, but stay tuned for future updates!")

setParent("triumph:interstellar/root")

setShowToast(false)
setAnnounceToChat(false)

addCriteria("main", "minecraft:impossible")